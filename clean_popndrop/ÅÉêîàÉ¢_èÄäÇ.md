# 💻 ПРИМЕРЫ КОДА ДЛЯ ДАЛЬНЕЙШЕЙ РАЗРАБОТКИ

## 1️⃣ Использование Supabase в компонентах

### Получить все данные из таблицы
```typescript
'use client'

import { useEffect, useState } from 'react'
import { supabase } from '@/lib/supabase'

export default function UsersList() {
  const [users, setUsers] = useState<any[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    const fetchUsers = async () => {
      const { data, error } = await supabase.from('users').select('*')
      if (error) console.error('Error:', error)
      else setUsers(data)
      setLoading(false)
    }

    fetchUsers()
  }, [])

  if (loading) return <div>Loading...</div>

  return (
    <ul>
      {users.map(user => (
        <li key={user.id}>{user.email} - {user.name}</li>
      ))}
    </ul>
  )
}
```

### Добавить новый элемент
```typescript
const { data, error } = await supabase
  .from('users')
  .insert([
    { email: 'new@example.com', name: 'New User', role: 'user' }
  ])
  .select()
```

### Получить элемент по ID
```typescript
const { data, error } = await supabase
  .from('users')
  .select('*')
  .eq('id', userId)
  .single()
```

### Обновить элемент
```typescript
const { data, error } = await supabase
  .from('users')
  .update({ name: 'Updated Name' })
  .eq('id', userId)
  .select()
```

### Удалить элемент
```typescript
const { error } = await supabase
  .from('users')
  .delete()
  .eq('id', userId)
```

---

## 2️⃣ Создание API маршрутов

### Создать новый API маршрут (app/api/users/route.ts)
```typescript
import { supabase } from '@/lib/supabase'
import { NextResponse } from 'next/server'

export async function GET() {
  const { data, error } = await supabase.from('users').select('*')
  
  if (error) {
    return NextResponse.json({ error: error.message }, { status: 400 })
  }
  
  return NextResponse.json(data)
}

export async function POST(request: Request) {
  const body = await request.json()
  
  const { data, error } = await supabase
    .from('users')
    .insert([body])
    .select()
  
  if (error) {
    return NextResponse.json({ error: error.message }, { status: 400 })
  }
  
  return NextResponse.json(data)
}
```

---

## 3️⃣ Работа с авторизацией (Supabase Auth)

### Вход пользователя
```typescript
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'user@example.com',
  password: 'password123'
})
```

### Регистрация пользователя
```typescript
const { data, error } = await supabase.auth.signUp({
  email: 'newuser@example.com',
  password: 'password123'
})
```

### Выход пользователя
```typescript
const { error } = await supabase.auth.signOut()
```

### Получить текущего пользователя
```typescript
const { data: { user } } = await supabase.auth.getUser()
```

---

## 4️⃣ Использование UI компонентов

### Button (уже установлен в components/ui/button.tsx)
```typescript
import { Button } from '@/components/ui/button'

export default function MyComponent() {
  return <Button onClick={() => console.log('Clicked')}>Click me</Button>
}
```

### Input (уже установлен в components/ui/input.tsx)
```typescript
import { Input } from '@/components/ui/input'

export default function MyComponent() {
  return <Input placeholder="Enter text..." />
}
```

### Card (уже установлен в components/ui/card.tsx)
```typescript
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function MyComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>My Card</CardTitle>
      </CardHeader>
      <CardContent>Content here</CardContent>
    </Card>
  )
}
```

### Form (уже установлен в components/ui/form.tsx)
```typescript
import { Form, FormField, FormItem, FormLabel } from '@/components/ui/form'
import { useForm } from 'react-hook-form'

export default function MyForm() {
  const form = useForm()

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)}>
        <FormField
          control={form.control}
          name="email"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Email</FormLabel>
              <input {...field} />
            </FormItem>
          )}
        />
      </form>
    </Form>
  )
}
```

---

## 5️⃣ Обработка ошибок

### Правильная обработка ошибок Supabase
```typescript
try {
  const { data, error } = await supabase.from('users').select('*')
  
  if (error) {
    console.error('Supabase error:', error.message)
    throw error
  }
  
  return data
} catch (err) {
  console.error('Unexpected error:', err)
}
```

---

## 6️⃣ Структура папок для расширения

```
app/
  api/                    # API маршруты
    users/
      route.ts           # GET, POST, PUT, DELETE
    posts/
      route.ts
  auth/                  # Страницы авторизации
    login/index.tsx
    signup/index.tsx
  dashboard/             # Защищенные страницы
    page.tsx
  layout.tsx             # Root layout
  page.tsx               # Home page

components/
  ui/                    # shadcn компоненты (уже установлены)
  forms/                 # Собственные формы
    UserForm.tsx
  tables/                # Собственные таблицы
    UsersTable.tsx
  dashboard/             # Dashboard компоненты
    Sidebar.tsx
    Header.tsx

lib/
  supabase.ts           # Supabase конфиг
  utils.ts              # Утилиты
  hooks/                # Custom hooks
    useUsers.ts
    useAuth.ts
```

---

## 💡 Полезные ссылки

- [Next.js Documents](https://nextjs.org/docs)
- [Supabase JavaScript Docs](https://supabase.com/docs/reference/javascript)
- [shadcn/ui Components](https://ui.shadcn.com)
- [React Hook Form](https://react-hook-form.com)
- [Tailwind CSS](https://tailwindcss.com)

---

## ⚡ Советы по оптимизации

1. **Используйте realtime подписки для live обновлений:**
```typescript
const subscription = supabase
  .from('users')
  .on('*', (payload) => {
    console.log('Change received!', payload)
  })
  .subscribe()
```

2. **Используйте React Query / Tanstack Query для кеша:**
```typescript
import { useQuery } from '@tanstack/react-query'

const { data: users } = useQuery({
  queryKey: ['users'],
  queryFn: () => supabase.from('users').select('*')
})
```

3. **Оптимизируйте изображения с Next.js Image:**
```typescript
import Image from 'next/image'

<Image src="/image.jpg" alt="Image" width={500} height={500} />
```

---

## 🎯 Возможные следующие шаги

1. Добавить встроенный вход через Google/GitHub
2. Создать защищенные маршруты с middleware
3. Добавить Real-time обновления
4. Интегрировать платежи (Stripe)
5. Добавить email уведомления

Успехов в разработке! 🚀
